// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model users {
  user_id  Int        @id(map: "pk_user_id") @default(autoincrement())
  fullname String     @db.VarChar(255)
  email    String     @unique() @db.VarChar(255)
  username String     @db.VarChar(255)
  password String     @db.Text()
  salt     String     @db.Text()
  token    String     @db.Text()
  messages messages[]

  @@index([user_id])
}

model messages {
  message_id   Int      @id(map: "pk_message_id") @default(autoincrement())
  title        String   @db.VarChar(255)
  content      String   @db.Text()
  created_date DateTime @default(now())
  user_id      Int
  users        users?   @relation(fields: [user_id], references: [user_id], onDelete: NoAction, onUpdate: Cascade, map: "fk_messages_user_id")

  @@index([message_id, user_id])
}
